<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.brewingmachine.mapper.ActivityMapper">

    <resultMap type="com.brewingmachine.entity.Activity" id="ActivityResult">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="type" column="type"/>
        <result property="rule" column="rule"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findById" parameterType="Long" resultMap="ActivityResult">
        SELECT * FROM activity WHERE id = #{id}
    </select>

    <select id="findAllActive" resultMap="ActivityResult">
        SELECT * FROM activity WHERE status = 'ACTIVE'
        ORDER BY end_time ASC
    </select>

    <select id="findByStatus" parameterType="String" resultMap="ActivityResult">
        SELECT * FROM activity WHERE status = #{status}
        ORDER BY create_time DESC
    </select>

    <select id="findByPage" resultMap="ActivityResult">
        SELECT * FROM activity
        <where>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%') OR description LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY create_time DESC
        <if test="page != null and size != null">
            LIMIT #{page}, #{size}
        </if>
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM activity
        <where>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%') OR description LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>

    <insert id="insert" parameterType="com.brewingmachine.entity.Activity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO activity (
            name, description, type, rule, start_time, end_time,
            status, create_time, update_time
        )
        VALUES (
            #{name}, #{description}, #{type}, #{rule}, #{startTime}, #{endTime},
            #{status}, NOW(), NOW()
        )
    </insert>

    <update id="update" parameterType="com.brewingmachine.entity.Activity">
        UPDATE activity
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="type != null">type = #{type},</if>
            <if test="rule != null">rule = #{rule},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE activity
        SET status = #{status}, update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM activity WHERE id = #{id}
    </delete>

</mapper>